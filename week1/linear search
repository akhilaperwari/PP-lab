 def linear_search(a,x,k):
       for i in range(len(a)):
          if(a[i] == x):
                return i
       return -1
   x =int(input("enter number of elements"))
   a = []
  for i in range(x):
      y=int(input("ele"))
      a.append(y)
  k=int(input("enter a value to be searched"))
  pos = linear_search(a,h,r)
  if(pos == -1):
      print("element not found")
  else:
      print("element to be found")
 
